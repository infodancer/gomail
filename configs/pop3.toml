# POP3 Server Configuration (Port 110, unencrypted)
command = "/usr/local/bin/pop3d"
args = ["-cfg", "/etc/gomail/pop3d.toml"]

[server]
server_name = "pop3.example.com"

[server.listener]
ip_address = ""
port = 110
max_connections = 50
idle_timeout = 600

[server.tls]
enabled = false
